window.addEventListener("load", (event) => {
    const menuMobile = document.querySelector('#ckeckboxMenuMobile');
    const menu = document.querySelector('#menu');
    
   /* menuMobile.addEventListener("click", () => {
        if (menuMobile.checked) { 
            menu.style.cssText = 'display: block;' 
        }
        if(!menuMobile.checked) menu.style.cssText = 'display: none;';

        console.log(menuMobile.checked)
    })*/
})